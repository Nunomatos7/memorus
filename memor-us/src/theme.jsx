import { createTheme } from '@mui/material/styles';
import '../src/assets/fonts/Popins.ttf';

const theme = createTheme({
  typography: {
    fontFamily: 'Poppins, sans-serif',
  },
});

export default theme;
